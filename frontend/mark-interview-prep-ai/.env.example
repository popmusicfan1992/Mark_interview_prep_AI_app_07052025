# Base API URL pointing to the Backend (leave blank to fallback to Vercel production URL)
VITE_API_BASE_URL=http://localhost:8000

# Google OAuth Client ID (Create a web app client ID in Google Cloud Console and add it here)
# Make sure to add http://localhost:5173 to your Authorized JavaScript Origins
VITE_GOOGLE_CLIENT_ID=
